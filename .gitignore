*.docx

# General Node.js / JavaScript (and React) project ignores
node_modules/           #  *Crucially Important*:  Your project's dependencies
npm-debug.log*          #  npm log files
yarn-debug.log*         #  Yarn log files
yarn-error.log*         #  Yarn error logs

# Build output directories (Client and Server, adjust paths as needed)
client/dist/                  #  Common for frontend build (React, Vite, etc.) and sometimes backend
client/build/                 #  Another common build output directory
client/out/                   #  Yet another build output directory

# Environment variable files (VERY IMPORTANT - SECURITY!)
.env                   #  Common file for environment variables
.env.local             #  Local environment overrides
.env.development.local #  Specific to development environment (if used)
.env.test.local        #  Specific to test environment (if used)
.env.*.local           #  Catch-all for other environment-specific local files

# Log files (usually not needed in Git)
*.log
logs
*.txt # Example: if you have other temporary text-based log/output files
report.xml # Example: Test reports

# OS-specific and editor files (junk files, irrelevant for collaboration)
.DS_Store              #  macOS folder metadata files
Thumbs.db              #  Windows thumbnail cache files
*.swp                  #  Vim swap files
*.swo                  #  Vim swap files
._*                    #  macOS resource forks
.Spotlight-V100        #  macOS Spotlight index
.Trashes                #  macOS Trash folder
ehthumbs.db             #  Windows thumbnail cache files
[Tt]emp*               #  Temporary files/folders (Temp, temp, Temporary, etc.)
[Tt]mp/                 #  Temporary files/folders (Temp, temp, Temporary, etc.)
*.tmp                 # Temporary files with .tmp extension
*.bak                 # Backup files
*.orig                # Backup files
*~                    # Backup files (editor backups often end with ~)

# IDE and Editor specific folders (adjust as needed for your IDE/editor)
.idea/                 # IntelliJ IDEA project files
.vscode/               # VS Code settings and workspace files
.project               # Eclipse project files
.settings              # Eclipse settings
*.suo                  # Visual Studio solution user options
*.user               # Visual Studio user files
*.sln.docstates       # Visual Studio solution file
*.csproj.user         # C# project user files

# Dependency manager specific files (if you use others)
jspm_packages/
web_modules/

# If you are using Docker (optional, if relevant to your project setup)
docker-compose.override.yml
docker-compose.yml.override
Dockerfile.lock
lerna-debug.log

# Custom project specific ignores (add any other files/folders unique to YOUR project that you don't want to track)
# Example: temp-data/